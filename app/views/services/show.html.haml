%h1 Title
%p= @service.name

%h3 Description
%p
  = @service.description

%h4 Details
%ul
  %li Current Version:
  %li Licence Type: 
  %li 
    Creator:
    = @organization.name
  %li= @service.created_at.strftime("Created: %m/%d/%Y")
  %li= @service.updated_at.strftime("Last update: %m/%d/%Y")
  %li 

%hr

%h4 How to use 
%p
  In order to use the
  = @service.name
  service, do the following things...


.service_headers
  %h4 Service Headers
  = @service.records.first.attributes.keys[1..-1]
  %ul
  - @service.records.first.attributes.keys[1..-1].each do |att|
    %li=att


.service_example_json
  %h4 Service Example
  = @service.records.first.attributes
