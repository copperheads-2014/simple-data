= render partial: "create_organization" if @user.organization.nil?
= form_for @service, multipart: true do |f|
  = f.label :name
  = f.text_field :name
  = f.label :description
  = f.text_field :description
  = f.file_field :file
  = f.submit "Import"