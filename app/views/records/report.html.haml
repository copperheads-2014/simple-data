%table
  %tr
    %th
      insertion_id
    -@headers.each do |header|
      %th
        =header.name
  -@records.each do |record|
    %tr
      -@attributes.each do |attribute|
        %td
          =record[attribute.to_sym]